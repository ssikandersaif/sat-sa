export default function RiskBadge({ score, level }) { const resolved = level || (score <= 20 ? 'Critical' : score <= 40 ? 'High' : score <= 60 ? 'Medium' : score <= 80 ? 'Low' : 'Healthy'); const tone = resolved === 'Critical' ? 'critical' : resolved === 'High' || resolved === 'Medium' ? 'watch' : 'stable'; return <span className={`risk-badge ${tone}`}>{resolved} · {Math.round(score)}</span> }
